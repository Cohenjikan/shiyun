# 换源对接准备 — 诗云 ← shiyun-corpus(2026-06-27 初步校验 / dry-run)

> 目标:把诗云的数据来源从**老原始仓**(`C:/corpus`:Werneror + yuxqiu + sheepzh,933,857 首)切换到**独立标注库 `shiyun-corpus`**(970,324 首,带 provenance + 体裁标注)。
> 本文 = **只读 dry-run** 的结论 + 对接方案。脚本:`scratchpad/corpus-dryrun.mjs`(只读,未动诗云任何数据)。

## 0. 结论(TL;DR)
**换源高度可行、破链极小。** 冻结字库不是障碍(99.8% 适配);现有诗人链接 32,652/32,657 原样保留(仅 5 个消失,且多为"修正");永久链 `#p=` 仅 1 首因勘误位移。净增 ~3.5 万首 + 806 位诗人 + 全量体裁标签。

## 1. dry-run 数字(新库 vs 诗云)
| 项 | 数值 |
|---|---|
| 新库总量 | **970,324**(public/PD 763,545 + restricted/现当代 206,779) |
| 体裁 | 诗 791,378 · 词 93,667 · 曲 374 · 新诗 84,905 |
| **字库适配**(body 全部汉字 ∈ 冻结 12,877) | **968,748 = 99.8%**;丢弃 1,576(99.7% 是新诗的库外字 1,575 + 1 词) |
| corrected_from≠null(会移动 `#p=` 的勘误) | **仅 1 首** |
| 净投影 · 仅 public | 763,545(< 当前 933,857,因当前把现当代内联了) |
| 净投影 · public+restricted | **968,748**(> 当前 933,857,**净 +34,891**) |

## 2. poetId 对账(链接安全)
新库与诗云**同一 poetId 方案**(`fnv32(name\|dynasty)`,陆游两边都是 `1ca4256c`)、**同一朝代码**,故按 id 直接比对:
- ✅ **保留 32,652 / 32,657(100.0%)** —— 现有 `#a=` 链接与星点原样不动。
- 🔴 **消失 5 个**(诗云有、新库无):`释證悟`(song,3)、`释了證`(song,1)= 繁体 `證` 残留,新库已规范化 `證→证`(是**修正**,非丢失,会以 `证`-形新 id 出现);`无名氏`(weijin,18)、`贺怜怜`(song,1)、`裴维安`(qing,62)= 匿名/零散。**可接受,或为这 5 个加 redirect 别名防 404。**
- ➕ **新增 806 位**:801 当代 + 少量古典;头部是诗云现缺的**民国/当代名家**:黄浚(1277)、连横(990)、龙榆生(608)、顾随(516)、黄侃(495)、李洸(466)…

## 3. 额外收益
- **体裁标签(诗/词/曲/新诗)随库自带** —— 直接满足 `DATA_SUPPLEMENT.md` 的 P0「体裁标注层」诉求(诗云现在 form=other 藏着 ~9.4 万词)。
- **provenance**(PD / 非商用 / 在版权)随库自带 —— 与本轮转的**非商业许可**口径一致,且能按需隔离敏感条目(叶嘉莹 2074、毛泽东 2027…)。

## 4. 待 owner 拍板(唯一决策)
**摄取哪些层?**
- **A. public-only(763,545,全 PD)**:最干净,但**会砍掉现当代**(比现在还少)——不推荐。
- **B. public + restricted(968,748)** ✅ **推荐**:保持并增强现有覆盖,诗云已是非商业许可、与 restricted 的非商用条款一致;具体在版权敏感条目(叶嘉莹等)用 provenance 过滤/隔离。

## 5. 对接实施方案(换源 = 下一步执行)
1. **写摄取适配器** `pipeline/ingest-corpus.mjs`:读 `shiyun-corpus/data/*.jsonl`(+`_restricted/`)→ 产出诗云 `poems/`+`poets.index`,**复用冻结字库**(库外字汉字的诗按现行规则 loud-skip,已知 1,576)、**沿用同一 poetId**(天然对齐)。保留 body 时按诗云规则剥标点。
2. **体裁入库**:把 genre 写进诗云数据/前端(新过滤维度),对齐 SUPPLEMENT P0。
3. **5 消失诗人**:加 redirect 别名(`释證悟`→证形 等)或接受。
4. **重建全链** → 对账:**charset hash 必须仍 `a392703b`**、manifest 计数、抽样验证**老 `#p=`/`#a=` 仍可复原**(至少抽 50 个高频)、5 消失项已处理、1 勘误首位移已知。
5. **部署**(走标准 dist 清理 + rsync;勿推 v1_backup)。
6. **回滚**:保留当前 `poems/`/`poets.index` 快照;出问题切回。

## 6. 红线复核(换源时必须守)
- 冻结字库 N=12,877 / hash `a392703b` 不变(本 dry-run 已确认新库 99.8% 落在其内,**不需要也绝不能扩字库**)。
- poetId = `fnv32(name\|dynasty)` 不变(已天然对齐)。
- FNV 四副本一致;古典层不去重;`poems[]` 顺序载荷。

> 状态:dry-run 完成,**未动诗云数据、未改 pipeline**。等 owner 定第 4 节的层选择,即可进入第 5 节执行。
